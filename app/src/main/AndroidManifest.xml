<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.zayer.shoppinglist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <provider
            android:name="pl.zayer.shoppinglist.contentprovider.ShoppingListsContentProvider"
            android:authorities="pl.zayer.shoppinglist.contentprovider" >
        </provider>

        <activity
            android:name=".activities.MainActivity"
            android:label="@string/activity_main_title"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ListDetailsActivity"
            android:label="@string/activity_list_details_title"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.zayer.shoppinglist.activities.MainActivity" />
        </activity>
    </application>

</manifest>